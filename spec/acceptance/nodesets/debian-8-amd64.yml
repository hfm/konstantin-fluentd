HOSTS:
  debian-8-amd64:
    platform: debian-8-amd64
    image: debian:8
    hypervisor: docker
    docker_preserve_image: true
    docker_image_commands:
      - 'apt-get install -y wget'
CONFIG:
  log_level: verbose
  type: foss
